# Application: API, validation, and the test suite. The ETL and analytics
# layers use only the standard library (csv, sqlite3, statistics).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pytest>=8.0
httpx>=0.27

# Data acquisition only (f1_fetch.py), and the one place this project touches
# the network. Not imported by the API, the ETL or the tests -- results.csv is
# committed, so the application builds and runs without it.
requests>=2.31
